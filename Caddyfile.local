:80 {
    root * /usr/share/caddy
    encode zstd gzip

    # Прокси на ваш бэкенд (сервис 'app' в этом же Compose)
    handle_path /api* {
        reverse_proxy app:8000
    }

    # Фронтенд (Vue/React router)
    handle {
        try_files {path} /index.html
        file_server
    }
}
